# CloudKeep gateway runtime dependencies.
#
# uvicorn[standard] pulls in uvloop, httptools, and the websockets stack used
# by run.py (loop="uvloop", ws_per_message_deflate=False). Pinned to floors
# that are known to support those options.
fastapi>=0.110
uvicorn[standard]>=0.20
bcrypt>=4.0
python-jose[cryptography]>=3.3
slowapi>=0.1.9
pydantic-settings>=2.1

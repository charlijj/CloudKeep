{
  "name": "cloudkeep-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "CloudKeep gateway SPA — build tooling for the noVNC bundle.",
  "scripts": {
    "build:novnc": "esbuild lib/novnc/core/rfb.js --bundle --format=esm --minify --legal-comments=none --outfile=lib/novnc.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}

# Secrets — provided out-of-band, never committed
.env
*.env
backend/src/.env
sys/vnc/passwd

# Python build artifacts
__pycache__/
*.py[cod]

# Node / frontend build tooling (the noVNC bundle artifact IS committed)
node_modules/

# Local VNC runtime artifacts
sys/vnc/*.log
sys/vnc/*.pid
